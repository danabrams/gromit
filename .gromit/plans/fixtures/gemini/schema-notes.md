# Gemini Schema Notes

## Token and Cost Observations

- Token and cost fields could not be observed because `gemini` is not installed in this environment.

## Model Observations

- `valid-model` attempt: command could not execute (`gemini` command not found).
- `invalid-model` attempt: command could not execute (`gemini` command not found).
