{"id":"rb-1771668571-gromit-sbgh6","timestamp":"2026-02-21T10:09:31.419996651Z","bead_id":"gromit-sbgh6","bead_title":"Add CategoryHardStopAction to inferCategoryFromText heuristic","spec_id":"","start_commit":"5735a391e961afb831a6c1526ef5ebc4f7b083cd","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n# Gromit\n\nA Go CLI tool that runs the Gromit loop correctly - with fresh context on each iteration.\n\n## Architecture\n\n- `internal/analyzer/` — failure analysis\n\n## Key Principles\n\n1. **Fresh context each iteration** — each Claude invocation is a new process\n2. **State in files, not memory** — bd beads + git commits are the memory\n3. **Model selection by complexity** — P0→opus, P1→sonnet, P2→haiku\n4. **Escalation on failure** — haiku→sonnet→opus retry chain\n5. **Separate validation** — tests/lint run as separate haiku invocation\n\n\n## Current Task\n\n**ID:** gromit-sbgh6\n**Title:** Add CategoryHardStopAction to inferCategoryFromText heuristic\n**Priority:** P2\n\n\n\n### Description\nIn internal/analyzer/analyzer.go, CategoryHardStopAction is recognized in parseAnalysisOutput for valid JSON but the heuristic fallback inferCategoryFromText (lines 250-269) has no case for detecting hard_stop text patterns. Non-JSON output mentioning a hard stop defaults to CategoryLogic instead.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched (e.g., `go test ./internal/foo/... ./internal/bar/...`), not the full suite. The separate validation phase runs `go test ./...` to catch cross-package regressions. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["GROMIT_TEST_TOUCHED_SHORT=1 ./scripts/test_touched.sh","./scripts/vet_touched.sh"],"failure_output":"refactor phase failed","failure_category":"","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668581-gromit-rzbee","timestamp":"2026-02-21T10:09:41.715809052Z","bead_id":"gromit-rzbee","bead_title":"Wire --choose-agent flag in refine command","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-rzbee\n**Title:** Wire --choose-agent flag in refine command\n**Priority:** P2\n\n\n\n### Description\nIn cmd/gromit/refine.go:43, the --choose-agent flag is registered but never read in runRefine(). The pipeline.RefineInput struct also lacks a ChooseAgent field. Compare explore.go which correctly reads and propagates this flag. The flag is a no-op for gromit refine.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668589-gromit-rzbee","timestamp":"2026-02-21T10:09:49.599245713Z","bead_id":"gromit-rzbee","bead_title":"Wire --choose-agent flag in refine command","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-rzbee\n**Title:** Wire --choose-agent flag in refine command\n**Priority:** P2\n\n\n\n### Description\nIn cmd/gromit/refine.go:43, the --choose-agent flag is registered but never read in runRefine(). The pipeline.RefineInput struct also lacks a ChooseAgent field. Compare explore.go which correctly reads and propagates this flag. The flag is a no-op for gromit refine.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668597-gromit-rzbee","timestamp":"2026-02-21T10:09:57.020032371Z","bead_id":"gromit-rzbee","bead_title":"Wire --choose-agent flag in refine command","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-rzbee\n**Title:** Wire --choose-agent flag in refine command\n**Priority:** P2\n\n\n\n### Description\nIn cmd/gromit/refine.go:43, the --choose-agent flag is registered but never read in runRefine(). The pipeline.RefineInput struct also lacks a ChooseAgent field. Compare explore.go which correctly reads and propagates this flag. The flag is a no-op for gromit refine.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668607-gromit-wy62","timestamp":"2026-02-21T10:10:07.926761655Z","bead_id":"gromit-wy62","bead_title":"Delete explore adapter and provider nil-check tests","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-wy62\n**Title:** Delete explore adapter and provider nil-check tests\n**Priority:** P1\n\n\n\n### Description\nDelete entire explore_pipeline_adapter_test.go file (28 dead tests). In internal/provider/provider_test.go, delete 7 tests using tautological 'var p Provider; if p == nil' skip pattern: TestProviderRunMethod, TestProviderStreamRunMethod, TestProviderRunValidationMethod, TestProviderIsUsageLimitErrorMethod, and 3 subtests in custom tier tests.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668615-gromit-wy62","timestamp":"2026-02-21T10:10:15.614124029Z","bead_id":"gromit-wy62","bead_title":"Delete explore adapter and provider nil-check tests","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-wy62\n**Title:** Delete explore adapter and provider nil-check tests\n**Priority:** P1\n\n\n\n### Description\nDelete entire explore_pipeline_adapter_test.go file (28 dead tests). In internal/provider/provider_test.go, delete 7 tests using tautological 'var p Provider; if p == nil' skip pattern: TestProviderRunMethod, TestProviderStreamRunMethod, TestProviderRunValidationMethod, TestProviderIsUsageLimitErrorMethod, and 3 subtests in custom tier tests.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668623-gromit-wy62","timestamp":"2026-02-21T10:10:23.447097254Z","bead_id":"gromit-wy62","bead_title":"Delete explore adapter and provider nil-check tests","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-wy62\n**Title:** Delete explore adapter and provider nil-check tests\n**Priority:** P1\n\n\n\n### Description\nDelete entire explore_pipeline_adapter_test.go file (28 dead tests). In internal/provider/provider_test.go, delete 7 tests using tautological 'var p Provider; if p == nil' skip pattern: TestProviderRunMethod, TestProviderStreamRunMethod, TestProviderRunValidationMethod, TestProviderIsUsageLimitErrorMethod, and 3 subtests in custom tier tests.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668633-gromit-yhvc","timestamp":"2026-02-21T10:10:33.311892047Z","bead_id":"gromit-yhvc","bead_title":"Delete scattered single dead tests","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-yhvc\n**Title:** Delete scattered single dead tests\n**Priority:** P1\n\n\n\n### Description\nDelete 8 dead test functions across 6 files: TestRunWithTimeout, TestStreamRunTimeout, TestMultipleContextLayers from claude_test.go; TestExploreCommand_UsesPipeline from explore_test.go; TestDecomposeWorkflow_RespectsContextCancellation from decompose_test.go; TestWriteIterationLog_PropagatesUsageLimited from usage_limit_field_test.go; TestExecGromit from chain_test.go; TestNewRunnerWithDepsWorksWithOnlyRouter from runner_claude_field_removal_test.go. Delete entire file if it becomes empty.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668640-gromit-yhvc","timestamp":"2026-02-21T10:10:40.947989728Z","bead_id":"gromit-yhvc","bead_title":"Delete scattered single dead tests","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-yhvc\n**Title:** Delete scattered single dead tests\n**Priority:** P1\n\n\n\n### Description\nDelete 8 dead test functions across 6 files: TestRunWithTimeout, TestStreamRunTimeout, TestMultipleContextLayers from claude_test.go; TestExploreCommand_UsesPipeline from explore_test.go; TestDecomposeWorkflow_RespectsContextCancellation from decompose_test.go; TestWriteIterationLog_PropagatesUsageLimited from usage_limit_field_test.go; TestExecGromit from chain_test.go; TestNewRunnerWithDepsWorksWithOnlyRouter from runner_claude_field_removal_test.go. Delete entire file if it becomes empty.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668648-gromit-yhvc","timestamp":"2026-02-21T10:10:48.754817733Z","bead_id":"gromit-yhvc","bead_title":"Delete scattered single dead tests","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-yhvc\n**Title:** Delete scattered single dead tests\n**Priority:** P1\n\n\n\n### Description\nDelete 8 dead test functions across 6 files: TestRunWithTimeout, TestStreamRunTimeout, TestMultipleContextLayers from claude_test.go; TestExploreCommand_UsesPipeline from explore_test.go; TestDecomposeWorkflow_RespectsContextCancellation from decompose_test.go; TestWriteIterationLog_PropagatesUsageLimited from usage_limit_field_test.go; TestExecGromit from chain_test.go; TestNewRunnerWithDepsWorksWithOnlyRouter from runner_claude_field_removal_test.go. Delete entire file if it becomes empty.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668658-gromit-yjkm5","timestamp":"2026-02-21T10:10:58.448373216Z","bead_id":"gromit-yjkm5","bead_title":"Add --spc flag to gromit status for detailed SPC dashboard","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-yjkm5\n**Title:** Add --spc flag to gromit status for detailed SPC dashboard\n**Priority:** P2\n\n\n\n### Description\ngromit status already shows a basic SPC summary (3 metrics + anomalies via formatSPCSummary in format.go). Add a --spc flag that expands this to show: all control limits with UCL/LCL, stratified per-provider metrics (when available), EWMA values (when available), Nelson rule violations (when available), leading indicators (input tokens, stall count, rate limit trends), rework rate, cost-per-completed-bead. The default (no flag) keeps the current compact summary. The --spc view should be a comprehensive SPC dashboard. Depends on the SPC improvement beads being implemented first, but the flag scaffolding and expanded display of existing metrics can land independently.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668665-gromit-yjkm5","timestamp":"2026-02-21T10:11:05.901688079Z","bead_id":"gromit-yjkm5","bead_title":"Add --spc flag to gromit status for detailed SPC dashboard","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-yjkm5\n**Title:** Add --spc flag to gromit status for detailed SPC dashboard\n**Priority:** P2\n\n\n\n### Description\ngromit status already shows a basic SPC summary (3 metrics + anomalies via formatSPCSummary in format.go). Add a --spc flag that expands this to show: all control limits with UCL/LCL, stratified per-provider metrics (when available), EWMA values (when available), Nelson rule violations (when available), leading indicators (input tokens, stall count, rate limit trends), rework rate, cost-per-completed-bead. The default (no flag) keeps the current compact summary. The --spc view should be a comprehensive SPC dashboard. Depends on the SPC improvement beads being implemented first, but the flag scaffolding and expanded display of existing metrics can land independently.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668673-gromit-yjkm5","timestamp":"2026-02-21T10:11:13.721163031Z","bead_id":"gromit-yjkm5","bead_title":"Add --spc flag to gromit status for detailed SPC dashboard","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-yjkm5\n**Title:** Add --spc flag to gromit status for detailed SPC dashboard\n**Priority:** P2\n\n\n\n### Description\ngromit status already shows a basic SPC summary (3 metrics + anomalies via formatSPCSummary in format.go). Add a --spc flag that expands this to show: all control limits with UCL/LCL, stratified per-provider metrics (when available), EWMA values (when available), Nelson rule violations (when available), leading indicators (input tokens, stall count, rate limit trends), rework rate, cost-per-completed-bead. The default (no flag) keeps the current compact summary. The --spc view should be a comprehensive SPC dashboard. Depends on the SPC improvement beads being implemented first, but the flag scaffolding and expanded display of existing metrics can land independently.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668683-gromit-z2k1g","timestamp":"2026-02-21T10:11:23.626093955Z","bead_id":"gromit-z2k1g","bead_title":"Fix PhaseMetric to use snapshotIterationUsage in all phases","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-z2k1g\n**Title:** Fix PhaseMetric to use snapshotIterationUsage in all phases\n**Priority:** P2\n\n\n\n### Description\nPhaseMetric red/refactor phases read raw bc.Result instead of snapshots, while the green phase already uses snapshotIterationUsage(). Apply snapshotIterationUsage() to all phases (red, refactor) to ensure per-phase deltas are recorded consistently instead of raw cumulative values. Due 2026-02-27.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668691-gromit-z2k1g","timestamp":"2026-02-21T10:11:31.361385827Z","bead_id":"gromit-z2k1g","bead_title":"Fix PhaseMetric to use snapshotIterationUsage in all phases","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-z2k1g\n**Title:** Fix PhaseMetric to use snapshotIterationUsage in all phases\n**Priority:** P2\n\n\n\n### Description\nPhaseMetric red/refactor phases read raw bc.Result instead of snapshots, while the green phase already uses snapshotIterationUsage(). Apply snapshotIterationUsage() to all phases (red, refactor) to ensure per-phase deltas are recorded consistently instead of raw cumulative values. Due 2026-02-27.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668803-gromit-z2k1g","timestamp":"2026-02-21T10:13:23.725627286Z","bead_id":"gromit-z2k1g","bead_title":"Fix PhaseMetric to use snapshotIterationUsage in all phases","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-z2k1g\n**Title:** Fix PhaseMetric to use snapshotIterationUsage in all phases\n**Priority:** P2\n\n\n\n### Description\nPhaseMetric red/refactor phases read raw bc.Result instead of snapshots, while the green phase already uses snapshotIterationUsage(). Apply snapshotIterationUsage() to all phases (red, refactor) to ensure per-phase deltas are recorded consistently instead of raw cumulative values. Due 2026-02-27.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668812-gromit-zbw39","timestamp":"2026-02-21T10:13:32.918333776Z","bead_id":"gromit-zbw39","bead_title":"Debug low-tier haiku 0ms failure and verify end-to-end routing","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-zbw39\n**Title:** Debug low-tier haiku 0ms failure and verify end-to-end routing\n**Priority:** P2\n\n\n\n### Description\nThe low-tier routing experiment generated zero qualifying iterations. The single haiku attempt (gromit-85lh.2) failed with 0ms duration and no provider recorded, suggesting a preflight or routing infrastructure failure. Fixed: process.go now uses escalation.SelectModel(r.cfg, b) for correct test-only bead model selection. Remaining work: add integration test verifying the full SelectTier(Low) → provider dispatch → model invocation chain works for both Claude (haiku) and Codex (codex-spark) providers. Leading indicator: at least 5 haiku or codex-spark iterations appear in iteration_metrics.jsonl within 24 hours of fix.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668820-gromit-zbw39","timestamp":"2026-02-21T10:13:40.555197281Z","bead_id":"gromit-zbw39","bead_title":"Debug low-tier haiku 0ms failure and verify end-to-end routing","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-zbw39\n**Title:** Debug low-tier haiku 0ms failure and verify end-to-end routing\n**Priority:** P2\n\n\n\n### Description\nThe low-tier routing experiment generated zero qualifying iterations. The single haiku attempt (gromit-85lh.2) failed with 0ms duration and no provider recorded, suggesting a preflight or routing infrastructure failure. Fixed: process.go now uses escalation.SelectModel(r.cfg, b) for correct test-only bead model selection. Remaining work: add integration test verifying the full SelectTier(Low) → provider dispatch → model invocation chain works for both Claude (haiku) and Codex (codex-spark) providers. Leading indicator: at least 5 haiku or codex-spark iterations appear in iteration_metrics.jsonl within 24 hours of fix.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668828-gromit-zbw39","timestamp":"2026-02-21T10:13:48.745288003Z","bead_id":"gromit-zbw39","bead_title":"Debug low-tier haiku 0ms failure and verify end-to-end routing","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-zbw39\n**Title:** Debug low-tier haiku 0ms failure and verify end-to-end routing\n**Priority:** P2\n\n\n\n### Description\nThe low-tier routing experiment generated zero qualifying iterations. The single haiku attempt (gromit-85lh.2) failed with 0ms duration and no provider recorded, suggesting a preflight or routing infrastructure failure. Fixed: process.go now uses escalation.SelectModel(r.cfg, b) for correct test-only bead model selection. Remaining work: add integration test verifying the full SelectTier(Low) → provider dispatch → model invocation chain works for both Claude (haiku) and Codex (codex-spark) providers. Leading indicator: at least 5 haiku or codex-spark iterations appear in iteration_metrics.jsonl within 24 hours of fix.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668840-gromit-zksej","timestamp":"2026-02-21T10:14:00.25014093Z","bead_id":"gromit-zksej","bead_title":"Add coverage validation template and RenderCoverageValidation method","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-zksej\n**Title:** Add coverage validation template and RenderCoverageValidation method\n**Priority:** P1\n\n\n\n### Description\nCreate `.gromit/templates/PROMPT_coverage_validation.md` instructing haiku to evaluate whether test code verifies a criterion and respond with `{\"covers\": true/false, \"reason\": \"one sentence\"}`. Define `CoverageValidationContext` struct and add `RenderCoverageValidation(ctx *CoverageValidationContext) (string, error)` to `Renderer` in `internal/prompt/prompt.go` and `PromptRenderer` interface in `internal/runner/interfaces.go`. Add tests to `internal/prompt/prompt_test.go`.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668847-gromit-zksej","timestamp":"2026-02-21T10:14:07.900747805Z","bead_id":"gromit-zksej","bead_title":"Add coverage validation template and RenderCoverageValidation method","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-zksej\n**Title:** Add coverage validation template and RenderCoverageValidation method\n**Priority:** P1\n\n\n\n### Description\nCreate `.gromit/templates/PROMPT_coverage_validation.md` instructing haiku to evaluate whether test code verifies a criterion and respond with `{\"covers\": true/false, \"reason\": \"one sentence\"}`. Define `CoverageValidationContext` struct and add `RenderCoverageValidation(ctx *CoverageValidationContext) (string, error)` to `Renderer` in `internal/prompt/prompt.go` and `PromptRenderer` interface in `internal/runner/interfaces.go`. Add tests to `internal/prompt/prompt_test.go`.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668855-gromit-zksej","timestamp":"2026-02-21T10:14:15.529480252Z","bead_id":"gromit-zksej","bead_title":"Add coverage validation template and RenderCoverageValidation method","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-zksej\n**Title:** Add coverage validation template and RenderCoverageValidation method\n**Priority:** P1\n\n\n\n### Description\nCreate `.gromit/templates/PROMPT_coverage_validation.md` instructing haiku to evaluate whether test code verifies a criterion and respond with `{\"covers\": true/false, \"reason\": \"one sentence\"}`. Define `CoverageValidationContext` struct and add `RenderCoverageValidation(ctx *CoverageValidationContext) (string, error)` to `Renderer` in `internal/prompt/prompt.go` and `PromptRenderer` interface in `internal/runner/interfaces.go`. Add tests to `internal/prompt/prompt_test.go`.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668866-gromit-zzvs5","timestamp":"2026-02-21T10:14:26.139446355Z","bead_id":"gromit-zzvs5","bead_title":"Implement CoverageTracker state machine","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-zzvs5\n**Title:** Implement CoverageTracker state machine\n**Priority:** P1\n\n\n\n### Description\nCreate internal/coverage/tracker.go with CoverageTracker, CriterionState, and Status types. Implement NewTracker, MarkCovered, RecordRejection, NextUncovered, IsComplete, UncoveredCriteria, UntestableCriteria, FormatCoverageState, and Summary. Create internal/coverage/tracker_test.go covering all state transitions and termination conditions.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668873-gromit-zzvs5","timestamp":"2026-02-21T10:14:33.930518602Z","bead_id":"gromit-zzvs5","bead_title":"Implement CoverageTracker state machine","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-zzvs5\n**Title:** Implement CoverageTracker state machine\n**Priority:** P1\n\n\n\n### Description\nCreate internal/coverage/tracker.go with CoverageTracker, CriterionState, and Status types. Implement NewTracker, MarkCovered, RecordRejection, NextUncovered, IsComplete, UncoveredCriteria, UntestableCriteria, FormatCoverageState, and Summary. Create internal/coverage/tracker_test.go covering all state transitions and termination conditions.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
{"id":"rb-1771668881-gromit-zzvs5","timestamp":"2026-02-21T10:14:41.404906513Z","bead_id":"gromit-zzvs5","bead_title":"Implement CoverageTracker state machine","spec_id":"","start_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","failure_commit":"66009a32ea95b149b1e15a52b480cc78ae5ac74e","prompt":"# Task Execution\n\nYou are executing a single task from the work queue. Focus only on this task.\n\n\n## Rules (Non-Negotiable)\n\n# Rules\n\nThese are non-negotiable constraints for this project.\n\n## Code Style \u003c!-- phases: build, review --\u003e\n\n- Use `go fmt` standard formatting\n- Use `error` returns, not panics, for recoverable failures. Exception: test helpers and init()\n- After unmarshaling or file loading, call `normalizeNilFields()` to convert nil slices to empty slices. Keep it as a pure nil→empty converter; field mapping belongs in a separate resolution step\n- Keep packages focused: one package should not reach into another package's internal types\n- `bead.Client`: `Ready()`/`CountReady()` return unblocked beads only (`bd ready`); `List()` returns all open beads (`bd list --status open`)\n- Functions that run subprocesses or prompts must inject those dependencies for testability\n- JSON tags must be snake_case and explicit (e.g. `input_tokens`, `cost_usd`); use `omitempty` only for truly optional fields\n- Agent execution must use `agent.Resolve()` + `agent.Launch()`; never construct `exec.Command` directly\n- Spec order: `scope.ValidateSpec(specsDir, specName)` before `scope.ResolveSpec(specName)`\n- Interface files must include `var _ InterfaceName = (*Impl)(nil)`. Interface changes must include implementation + mock updates in the same bead\n- Prompt templates: `PROMPT_\u003cname\u003e.md` files registered in `runInit()` via `defaultXxxTemplate` constants; renderers take context structs returning `(string, error)`. Share common sections; vary only Instructions/Completion per context type\n- Router: choose tier (`low|medium|high`), resolve model (`haiku|sonnet|opus`). Store model names (not tier labels) in fields like `EscalatedTo`. Lives in `internal/runner/escalation/`\n- Mocks use FnField pattern: optional function-pointer fields with nil-safe defaults. Tests set only the callbacks needed for the code path under test; don't require full mock setup for a single method\n- Per-run accumulator fields (slices, maps) in Runner must be reset at the top of Run(), not in individual phases\n- Renderer owns context/state setup. Configure it in NewRunner() via setters; BuildContext() reads those values. Never bypass Renderer state\n\n## Safety \u003c!-- phases: build, review --\u003e\n\n- Never commit secrets, API keys, or credentials\n- Never delete data without explicit confirmation in the spec\n- Pass large strings to Claude CLI via temp files, not CLI arguments (avoids ARG_MAX)\n- Shell scripts with user content: use quoted \u003c\u003c'EOF' heredocs; pass dynamic values as arguments, not string interpolation\n- Go subprocesses with user-influenced args (bead IDs, refs, branch names) must use `runArgv`, not `runCmd`, to prevent shell/flag injection\n\n## Test Quality \u003c!-- phases: build, review --\u003e\n\n- Acceptance tests must verify behavior through public API/CLI surfaces, not private helpers\n- Do not test Go stdlib behavior (`os.MkdirAll`, `os.WriteFile`, `json.Marshal`). Trust stdlib; test your code\n- Never use `t.Skip()` — use `//go:build acceptance` for tests requiring external dependencies\n- 2+ tests sharing 10+ lines of setup: extract a setup helper. 3+ tests with same structure/different inputs: use table-driven tests\n- Prefer compile-time checks or behavioral tests over `os.ReadFile`+`strings.Contains` on `.go` source files — source-reading tests break on refactoring\n- `*_acceptance_test.go` files must have `//go:build acceptance` or test end-to-end CLI behavior. Unit tests verifying acceptance criteria go in `*_test.go`\n- Acceptance tests (`//go:build acceptance`) have a 6,000-line total budget; prefer unit tests and account for this in specs\n\n## Terminology \u003c!-- phases: build, review, refine, plan --\u003e\n\n- \"Backlog\" always means the ideas backlog (`gromit add` / `.gromit/backlog.jsonl`), not beads. Beads are work items; backlog items are raw ideas awaiting refinement\n\n## Architecture \u003c!-- phases: build, review --\u003e\n\n- `internal/runner/*/` sub-packages must not import siblings; cross-cutting types live in `runtypes/`. Parent `runner` package uses type aliases for backward compatibility. Production files: \u003c550 lines; facade files: \u003c1000 lines\n- Interactive commands use the session worktree lifecycle: package-level launcher fn var, session command const, `cfg.Worktree.IsEnabled()` opt-out, `sessionConflictSettingsFromConfig`, `runWithSessionWorktreeWithConflictSettings`. Lifecycle: create worktree → callback → record pending branch → merge attempt → cleanup or conflict handoff\n\n## Build Process \u003c!-- phases: build --\u003e\n\n- Pipeline methods: typed input/output structs, validate deps first, renderer processing, post-processing change detection. Keep pipeline tests next to command files\n- Config fields: defaults in `SetDefaults()` (use `*int`/`-1` when zero is valid), test omitted-YAML defaults. Mirror new `IterationResult` fields into `IterationLog` in `writeIterationLog()`\n- Shared-package refactors (e.g., learnings/config): rerun all affected dependent test suites after each commit; verify each diff still matches intent\n- Test-only bead detection: use `bead.IsTestOnlyBead()` (e.g., \"Add tests for\") alongside `IsMethodologyActive()`\n- On bead failure: add to `skippedBeads`, not inline. After 3+ cross-run consecutive failures (JSONL-tracked via `MaxCrossRunFailures`), skip and surface for review/decomposition\n- `Run()` order: validate → execute → persist state → between-iteration hooks → continue. No reordering; log timeout warnings (not early return); nil-safe receiver/config checks at method entry\n- New config types/fields: update `gromit.yaml` to match — project-config tests validate the live file against the schema\n- Validation recovery: auto-fix (`gofmt`/`goimports`) first, re-validate, then Claude escalation only if still failing (`MaxValidationRetries`, default 1)\n- `test/contracts/` contract tests verify git call order (rev-parse before `git diff --stat`); keep harness init and sequencing intact\n- Validation commands in gromit.yaml must match the build system (check go.mod/package.json). For this project: `go test`, `go vet`, `go build` only — never pnpm/npm\n- Build phases: run test/vet on touched packages only. Full validation: `go test ./...`, `go vet ./...`, `go build ./...`\n- `test_touched.sh` tests all branch-modified packages. Pre-existing failures in touched packages block new beads — verify target packages pass before starting dependent work\n\n## Decomposition \u003c!-- phases: plan --\u003e\n\n- Split beads touching 6+ files across unrelated packages; target 1-3 files per leaf bead. For large facades (1000+ lines), batch 3-5 methods per bead. Decompose 5+ levels deep for narrow scope. Skip single-child decompositions (already atomic)\n- Never split natural units: Interface + implementation + mock updates, implementation + tests, companion methods, command flags+wiring, or template+registration\n- If scope too broad, decompose instead of escalating tier. If apex tier times out or fails, split into sub-beads and retry\n- Pre-split broad work (titles containing infrastructure/E2E/consolidate/extract/shared/refactor, or 3+ new types plus behavior). Decomposition orders: test infra (fixtures→helpers→tests), interface extraction (interface→mocks→callers), package extraction (create→move→wire→remove), helper extraction (pure→dependency-heavy→wiring)\n\n## Retro Formatting \u003c!-- phases: retro --\u003e\n\n- `LEARNINGS.md` headers: `### YYYY-MM-DD | DESCRIPTIVE_TITLE | CATEGORY_NAME`. Titles must be concise pattern descriptions (not bead IDs or vague words like \"final\"). Consolidated entries: add `*Related to: id1, id2*` line\n\n\n\n\n\n\n\n\n\n## Project Context\n\n\n\n## Current Task\n\n**ID:** gromit-zzvs5\n**Title:** Implement CoverageTracker state machine\n**Priority:** P1\n\n\n\n### Description\nCreate internal/coverage/tracker.go with CoverageTracker, CriterionState, and Status types. Implement NewTracker, MarkCovered, RecordRejection, NextUncovered, IsComplete, UncoveredCriteria, UntestableCriteria, FormatCoverageState, and Summary. Create internal/coverage/tracker_test.go covering all state transitions and termination conditions.\n\n\n\n\n\n\n\n\n## Instructions\n\n1. **Study the codebase** before making changes - don't assume code is missing\n2. **Implement the task** following existing patterns in the codebase\n3. **Write tests** if the task involves new functionality\n4. **Self-check** — Before completing, run `go test` and `go vet` scoped to the packages you touched using this exact command: `go test ./internal/analyzer/...`. Fix failures before committing\n5. **Commit your changes** with a clear commit message\n\n## Completion\n\nWhen the task is complete:\n- All code changes are committed\n- Tests pass (if applicable)\n- The implementation matches the specification\n\nDo NOT output any special completion markers - just complete the task and exit.\n","validation_commands":["go test ./internal/analyzer/..."],"failure_output":"red phase: invocation failed: ","failure_category":"other","escalation_chain":["haiku","sonnet","opus"],"env":{"go_version":"go1.26.0","os":"linux","arch":"amd64"}}
