# Bug Investigation: Bead timeout — Slim Deps to Router-only

## Symptom
Bead `gromit-igprl.2` ("Slim Deps to Router-only and update newRunnerWithDepsImpl") timed out after 40 minutes with zero code changes (commit range is identical: `f513b70..f513b70`). The bead never completed any work.

## Root Cause
**Scope underestimation causing timeout.** The bead description estimated "26 test files (~130 call sites)" but the actual codebase has **40 test files with ~342 call sites** referencing `NewRunnerWithDeps`, `Deps{`, or `runner.Deps{}`. The required changes span three dimensions that compound:

1. **Structural change**: Remove 12 fields from `Deps` struct (keeping only `Router`)
2. **Return type change**: `newRunnerWithDepsImpl` currently returns `*Runner` but needs to return `*Orchestrator`
3. **Mass test update**: 342 call sites across 40 files need mechanical updating

The combination of understanding the new wiring pattern, updating the constructor logic, AND updating 342 call sites exceeds a single bead's 40-minute budget.

### Breakdown by file category:
- **Runner package tests**: 28 files, 291 occurrences (largest: `interfaces_test.go` at 79)
- **Acceptance tests**: 4 files, 25 occurrences
- **Pipeline package tests**: 7 files, 47 occurrences
- **CLI package tests**: 2 files, 4 occurrences

## Affected Code
- `internal/runner/constructor_with_deps.go:22-36` — `Deps` struct (13 fields → 1)
- `internal/runner/constructor_with_deps.go:40-131` — `newRunnerWithDepsImpl` (returns `*Runner`, needs `*Orchestrator`)
- `internal/runner/runner.go:120-122` — `NewRunnerWithDeps` public wrapper
- 40 test files across `internal/runner/`, `internal/pipeline/`, `cmd/gromit/`

## Suggested Fix
Decompose into 4-5 smaller beads:

1. **Core constructor rewrite** (1-2 files): Change `Deps` struct and `newRunnerWithDepsImpl` to build stages from Router + Config, return `*Orchestrator`. This is the architectural change.

2. **Update runner package tests — batch 1** (~14 files): Update the largest test files (`interfaces_test.go`, `methodology_wiring_test.go`, `runner_test.go`, etc.) to use `Deps{Router: provider.NewSingleProviderRouter(fakeProvider)}`.

3. **Update runner package tests — batch 2** (~14 files): Update remaining runner test files.

4. **Update acceptance + pipeline + CLI tests** (~12 files): Update acceptance tests, pipeline tests, and CLI tests.

Each sub-bead stays under 15 files and can complete within the time budget. The first bead must land before the test-update beads can start (dependency).

## Evidence
- Commit range `f513b70..f513b70` confirms zero progress before timeout
- `grep -r` counts: 40 unique test files, 342 total references to Deps construction
- Current `Deps` struct at `constructor_with_deps.go:22-36` has 13 fields
- `Orchestrator` already exists at `orchestrator.go:61-63` with full 5-stage pipeline support
- All 5 stage packages exist and are implemented (`prepare/`, `execute/`, `validate/`, `review/`, `epilogue/`)
- Adapter types exist in `adapters.go` for bridging `provider.Router` to stage interfaces
