# Bug Investigation: Remaining Test Failure Groups After Session Worktree/Add Fixes

## Symptom
After fixing the original `runWithSessionWorktree` regression and related stdin/cwd test instability, full validation still fails in grouped areas unrelated to the original failure.

## Root Cause
The remaining failures appear to be pre-existing or orthogonal issues grouped into four buckets:

1. Reclassified review-spec acceptance tests refer to a missing target file.
2. Queue command tests exhibit output instability/shared-state leakage across runs.
3. CLI smoke guard enforces thin-wrapper import policy currently violated by production files.
4. Prompt phase budgets exceed configured char limits in `internal/prompt`.

## Affected Code
- `cmd/gromit/review_spec_validation_refactor_reclassified_test.go`
- `cmd/gromit/queue_test.go`
- `cmd/gromit/cmd_smoke_slimming_test.go`
- `cmd/gromit/interactive_worktree.go`
- `cmd/gromit/tui.go`
- `cmd/gromit/vision_metrics.go`
- `internal/prompt` (phase budget tests and prompt/rules sources)

## Suggested Fix
### Group 1: Reclassified review-spec tests (missing file)
- Decide whether `cmd/gromit/review_spec_validation_reclassified_test.go` should exist.
- If yes: add/restore that file with required helpers/sentinels expected by the refactor tests.
- If no: update or retire `review_spec_validation_refactor_reclassified_test.go` expectations.

### Group 2: Queue output instability/delegation
- Isolate mutable globals and command flags in queue tests (`queueCmd` flags, shared output config, formatter toggles).
- Ensure deterministic output mode per test (explicitly set flags, reset after each run).
- Verify no test mutates global queue presentation state without cleanup.

### Group 3: Thin-wrapper import guard
- `cmd_smoke_slimming_test` currently flags:
  - `cmd/gromit/interactive_worktree.go` importing `internal/integrationqueue`
  - `cmd/gromit/tui.go` importing `internal/tui`
  - `cmd/gromit/vision_metrics.go` importing `internal/visionmetrics`
- Either:
  - refactor wiring into adapter/deps boundary the smoke test expects, or
  - intentionally update smoke-policy tests if architecture changed.

### Group 4: Prompt phase char budgets
- `internal/prompt` fails `TestRulesPhaseCharBudgets` (red/build/green/refactor/review over budgets).
- Reduce included prompt/rules text per phase or adjust budget fixtures/tests if policy changed intentionally.

## Evidence
- `go test ./cmd/gromit -count=1` failures include:
  - `TestReviewSpecValidationRefactor_*` missing `cmd/gromit/review_spec_validation_reclassified_test.go`
  - `TestQueueCmd_RegressionAssertion_OutputIsStable`
  - `TestShowQueue_DelegatesToPipeline`
  - `TestCmdSmoke_AcceptanceTestsEnforceThinWrapperDelegation`
- `go test -vet=off -p 4 -parallel 4 ./...` also fails:
  - `internal/prompt: TestRulesPhaseCharBudgets` with per-phase budget overruns
- `go vet ./...` passed
- `go build ./...` passed

## Session Scope Completed
This session fixed the original failure class and related deterministic issues in:
- session worktree queue metadata stubbing/tests
- add-command stdin reader handling bug
- command-test cwd restoration hardening

